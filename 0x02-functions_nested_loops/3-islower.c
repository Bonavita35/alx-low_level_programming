#include "main.h"

/**
 * _islower - Check for lowercase ASCII char
 * Return: 0 if not lowcase, 1 if lowcase
 */
int _islower(int c)
{
	if ((c >= 80 && (c <= 143))
	{

		return (1);
	}
	return (0);
}
